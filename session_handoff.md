# 提案書生成エージェント — セッション引き継ぎメモ

> **用途**: 新しいClaude Code/会話セッションにこのファイルを渡し、文脈を引き継ぐためのもの。
> 「このファイルを読んで、続きから進めて」と言えば、ゼロから説明不要で再開できる。
> 関連: `slide_design_handbook.md`（設計原則の教科書）も同じリポジトリにある。

---

## 1. これは何のプロジェクトか
顧客向け**提案書（パワーポイント）を生成するエージェント**を作っている。
最終成果物は **.pptx（客先提出物）。品質は絶対に落とせない。一貫性が最優先。**
凝ったアニメは不要。必要なのは「データ反映」と「決まったテンプレートの再現」。

---

## 2. 根本となる設計原則（一貫して効いている考え方）
- **見た目の物理パラメータ（フォント・余白・色・配置）はモデルに決めさせず固定**。中身だけ可変。
- **不変条件（守るべき品質）は固定、手段（面積%など）は固定しない**。
- **客観品質＝機械チェックで保証／主観品質＝judge＋人の最終確認**。人確認は必須（外すと破綻）。
- **思考と成果物を混ぜない**（思考はthinking、成果物は構造化出力）。
- **分類より判断**。ただし「区別すると必要な中身が本当に変わる軸」だけ採用する。
- **AIに書かせる最大リスク＝事実の捏造**。事実（データソース由来・要検証）と表現（AIに任せてよい）を分ける。

---

## 3. 品質の作り方（二層）
- **① ハード制約（機械判定）**: フォント階層・余白・色数・文字数上限・データ整合・未確定情報の排除 等。
- **② ソフト基準（ルーブリック採点）**: ストーリー・刺さり・簡潔性 等。良例(exemplar)と比較すると安定。
- 既に `quality_spec.md` 等にルール化済み（reconcile実施済み。バックアップ commit 0bf2ec4）。
- **育て方（LEARN）**: 人の修正→「specのどこに当たるか、無ければ追記」。修正の都度ルール/良例に還元。
  - 注意: 現状この還元は自動化されていない。会話履歴(jsonl)・git diffに生ログは残るので後から回収可能。
  - 区切りで「今回の修正をルールに還元してcommitして」と言うのが最小コストの溜め方。

---

## 4. レイアウトの作り方（重要な結論）
- **デザインはコードで"再現"しない。元の.pptxを"そのまま使い"、データだけ差し替える**＝忠実度100%。
  - 前回「ローカルで品質再現は無理」だったのは、コードでレイアウトをゼロから組もうとしたから。
  - 正解: **複雑レイアウトはPowerPointで作り込む／python-pptxは埋めるだけ**（作るのは苦手、埋めるのは得意）。
- **アーキタイプ=語彙（部品）**。skeletonで構成を固定するのではなく、組み合わせて使う。
  - 現状カバレッジ76%。追加決定: `two_col_hybrid` と `cards`(2〜6項目) を provisional追加。
    `image_statement`は画像主役なので `image_lead` に統合（新規不要）。
- **用途の使い分け＝「何を売るか（ゴール）」別のデッキ・レシピ**（例: 人を売る / 会社を売る / ソリューション）。
  共有スライド・ライブラリ＋ゴール別レシピ（必須スライド・順序・トーン・禁止）。ゴールは人が宣言。
  ※ 5分類・ニーズ/シーズの「分類」は無意味として却下済み。ゴール軸だけは意味があるので採用。

---

## 5. 現在の技術的な取り組み（ここが今のメイン）
**Code完結型（ローカル python-pptx）＋実測ベースのレイアウト検証ループ**を構築中。

- **状況**: 元データの完全再現は成功。**データをずらすとボックスはみ出し・レイアウト崩れが発生**。
- **対策（指示済み）**:
  - ボックスごとに **auto_size=NONE（伸びない）／max_chars（実測で固定）／min_font** を持つ。
  - 要素を **anchored（不動）/ flow（伸縮可）** でタグ付け。崩れの波及をflow+予約余白に限定。
  - はみ出し処理は**優先順位ラダー**: ①短縮 ②min_fontまで縮小 ③予約余白へ ④繰り返し要素は再配置 ⑤無理ならエラー停止（黙って崩さない）。
  - 改行は**文節境界**（単語途中で割らない）＋禁則ON。
- **検証方法の切り替え（指示済み）**:
  - はみ出し/崩れ判定を **スクショ＋ビジョン(LLM)→ コードで実測** に変更（タダ・速い・決定的）。
  - レンダリングは **PowerPoint COM自動化(win32com)** が最高忠実度（最終がPowerPointのため）。PowerPointは導入済み。
    - 残確認: `python -c "import win32com.client; win32com.client.Dispatch('PowerPoint.Application'); print('COM OK')"`
      → ダメなら `pip install pywin32`。
  - 実測する不変条件: はみ出し0（占有率上限90〜95%）/ 衝突0 / フォント≥min_font / anchored不動 / flow範囲内。
  - ビジョン/目視は「見栄え（美的）」の最終スポットチェックにだけ残す。
- **検証データ**: 元データだけでなく**ストレスデータ**（最短/典型/最長/超過/件数変動＋「長文×多件数」の組合せ）で回す。
  実物が出した崩れは都度テストに追加して育てる。

---

## 6. 環境・運用メモ
- **Windows**でClaude Codeをローカル実行。**API не使用、サブスク認証**。
  - ヘッドレス/サーバーから使うなら `claude setup-token` → `CLAUDE_CODE_OAUTH_TOKEN` を環境変数に。`--bare`は使わない。
  - PythonからCLIを呼ぶ場合は `-p`、`--permission-mode acceptEdits`、フルパス指定(`where claude`)、env明示。
  - Webから本番生成する場合は同期subprocessでなくバックグラウンドジョブ推奨。
- **本番のpptx生成は Claude design(HTTP)を通さずローカルpython-pptxで**（HTTP変換で数字・配置がズレるため）。
  - Claude designは「テンプレートを一度ラリーで完璧化する時」だけ使い、確定後はネイティブ.pptxに凍結。
- 使用量対策: 段階ゲート（STAGE0 文字で構成確認→数枚デモ→合格後に全体）。合格まで全体を作らない。
- コンテキスト満杯で頻繁に圧縮が走る → テーマごとに `/clear`、状態はファイル＋gitに退避、CLAUDE.mdに恒久指示。

---

## 7. いま一番のボトルネック（技術ではない）
- **良い事例スライドが実質1枚しかない**。増やしたいが上司からなかなかもらえない。
- 解決の順番: **先に"1本"良いアウトプットを出す**（手作業/design併用でよい）→ それを証拠に事例提供と信頼を得る→ 事例が増えてから自動化・テンプレ化を本格化。
- 1本の良い成果物が「納品物・上司への証拠・2枚目の事例」の三役を果たす。

---

## 8. 次にやること（優先順）
1. `COM OK` を確認（pywin32）。OKなら **COM描画＋コード実測**の崩れ検証を実装し、ビジョン判定を置換。
2. **はみ出しラダー＋anchored/flow＋max_chars(実測)** を実装し、**ストレスデータ**で検証ループを回す。
3. **実際に出す提案1本**を仕上げて世に出す（事例とバイインの起点）。
4. 事例が増えたら **ゴール別レシピ**（人/会社/ソリューション）を整備。
5. 日本語の句読点・文長・助詞チェックに **textlint（ja-technical-writing）** を導入。
6. 修正の都度、ルール/良例へ還元（LEARN）を習慣化。

---

## 9. 関連ファイル
- `slide_design_handbook.md` — 出典つき・役割別・機械チェック可能な「正しいパワポ設計」の教科書。
- `quality_spec.md` / `archetypes.md` / `profiles.md` / `AGENT_SPEC.md` 等 — エージェント本体側（別リポジトリ/別PC）にある想定。
- このメモと handbook は `withharuka` リポジトリ、ブランチ `claude/ppt-agent-thinking-split-TSxSv` にある。
  ※ エージェント本体は別フォルダ/別リポジトリのため、git pullではなくファイルを移して使うこと。
