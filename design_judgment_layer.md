# デザイン判断レイヤー仕様（design judgment layer）

> **位置づけ**: 提案書生成エージェントの「埋める前」に挟む工程。
> データを**どのレイアウトで・どう見せるか**を決める。デザインを *生成* するのではなく、
> もらった実績テンプレ（語彙）から *選び・並べ替え・図解化* する **判断** をする層。
>
> **これが解決する課題**: 「コードで埋めるだけ」だとデザインの意図が乗らず質が低い。
> その手前に判断工程が無いのが原因。生成器（Claude design 等）を足す問題ではない。
>
> 関連: `slide_design_handbook.md`（設計原則）／`quality_spec.md`（品質ルール）／`archetypes.md`（部品語彙の本体）。
> 本書はそれらを束ねる「選択の頭脳」を定義する。

---

## §0. パイプライン上の位置

```
データソース
   │
   ▼
[正規化] 構造化中間表現（事実は検証必須）
   │
   ▼
┌─────────────────────────────────────────────┐
│ ★デザイン判断レイヤー（本書）                 │
│  1. ロール判定（§1: cover/content/data…）     │
│  2. アーキタイプ選択（実績テンプレ＝語彙）      │
│  3. 内容→ビジュアル写像（箇条書き→図/表/KPI）  │
│  4. タイトル主張文化・体言止め・短縮           │
│  出力 = slide_plan（構造化・決定的に検証可能）  │
└─────────────────────────────────────────────┘
   │
   ▼
[STAGE0 検証] テキスト段階で機械チェック（安い・速い）← ここで止める
   │ 合格
   ▼
[埋め込み] python-pptx が slide_plan を凍結テンプレに流し込む
   │
   ▼
[決定的検証] COM実測：はみ出し0・色数≤3・占有率… → はみ出しラダーで自己修正
   │
   ▼
[主観採点] judge ＋ 人の最終確認 → 凍結・納品
```

**要点**: 判断レイヤーの出力は「絵」ではなく **構造化された設計図（slide_plan）**。
埋め込みは決定的に行えるので忠実度が保てる。

---

## §1. 役割と責任の分離（誰が何を決めるか）

| 主体 | 決めること | 決めてはいけないこと |
|---|---|---|
| **人** | ゴール宣言（人/会社/ソリューションを売る）・最終承認・テンプレ供給 | 一枚ごとの作業 |
| **LLM（本層）** | ロール判定・アーキタイプ選択・内容→図解写像・主張文化・短縮 | フォント/色/座標の発明、数字の創作 |
| **コード** | 制約適用・slide_plan検証・テンプレ埋め込み・実測 | 主観的な「良し悪し」 |

> **不変条件**: LLMは「**既存部品から選ぶ・並べ替える**」のみ。**ピクセルを発明させない**。
> **事実（データ由来・要検証）と表現（LLMに任せてよい）を各スロットでタグ分け**し、事実の捏造を防ぐ。

---

## §2. アーキタイプ語彙（選択肢のカタログ）

> 実体は `archetypes.md`。本書は **選択基準** を定義する。各アーキタイプに
> `【適用ロール】` と `【選択条件（機械判定可）】` を持たせる。供給テンプレが増えるほど語彙が増える。

| archetype | 主な用途 | 適用ロール | 選択条件（目安・機械判定寄り） |
|---|---|---|---|
| `title_only` | 表紙・章扉 | cover, section | 本文なし。大きな文字＋広い余白 |
| `statement` | 主張1つを大きく | content, summary | キーメッセージ＋視覚根拠1点。本文塊なし |
| `bullets` | 並列の要点 | content | 項目 2〜5。各項目が短文/体言止め |
| `two_col` | 対比・前後 | content | 2つの対の比較（Before/After, 課題/解決） |
| `two_col_hybrid` | 文＋ビジュアル併置 | content | 左に主張、右に図/画像（provisional） |
| `cards` | 並列カード | content | 項目 2〜6。各項目が見出し＋一言＋アイコン（provisional） |
| `kpi_metric` | 数値を主役 | data, company_intro | 数値 1〜3個。棒グラフにしない |
| `chart` | データ可視化 | data | データ点 ≥3。メッセージ型に応じ棒/折れ線/散布… |
| `matrix` | マトリクス/2×2 | content, data | 2軸での分類・比較 |
| `process_flow` | 手順・時系列 | content | 順序のあるステップ 3〜6 |
| `table` | 密な表 | data | 行×列が多くセル充填率 ≥80% |
| `image_lead` | 画像が主役 | content, self_intro | 画像が主、文は最小（image_statement を統合） |

---

## §3. 選択ロジック（決定木）

### Step 1 — ロール判定（最初に必ず）
`slide_design_handbook.md §1` のロール表で分類。
- **儀礼・構造系**（cover/greeting/self_intro/company_intro/agenda/section/cta_contact）
  → 厳格ルール免除。候補は `title_only` / `image_lead` / `kpi_metric`（会社紹介の実績数値）等に限定。**余白・大きな文字が正解**。
- **主張系**（content/data/summary）→ Step 2 へ。

### Step 2 — 主張系のアーキタイプ選択（内容の形で決める）

優先順に評価（最初に一致したものを採用、外れたら escape hatch §6）：

1. **メッセージ型 → チャート**（§4-1 Zelazny）
   - 比較→棒 / 時系列→折れ線 / 構成→積み上げ / 相関→散布 / 順位→並替え棒。`chart`
2. **データ量で見せ方**（§4-2 スカスカ防止）
   - 数値1つ → `kpi_metric`（棒にしない）
   - 比較2〜3 → `two_col` or `cards`
   - 行が多い → `table`（充填率≥80%）／少ない → `cards`/`kpi_metric`
3. **内容の性質 → 図解**（§4-5 Duarte）
   - 手順/時系列 → `process_flow`
   - 2軸分類 → `matrix`
   - 並列の要点 → `cards`（見出し付き）or `bullets`（2〜5・短文）
4. **画像が主役** → `image_lead`
5. **主張1つを強く** → `statement`

### Step 3 — タイトル整形（全主張系スライド）
- 体言止め（「〜について」「〜の概要」）を検出 → **主張文（言い切り）に書き換え**（§2-3）。
- 13文字理想・最大20字（§3-3）。

### Step 4 — 内容→スロット写像
選んだ archetype のスロットに内容を割付。各スロットで：
- `max_chars` を超えたら **短縮（体言止め/キーワード化）**。フォント縮小で逃げない。
- 各スロットに `source: fact|expression` を付与（事実は要検証フラグ）。

---

## §4. 内容→ビジュアル写像ルール（箇条書き撲滅）

| 入力の形 | 既定の出力 | 根拠 |
|---|---|---|
| 数値1つ | `kpi_metric`（巨大数字） | §4-2 |
| 手順・段階 | `process_flow` | §4-5 |
| A vs B | `two_col` / `matrix` | §4-5 |
| 5項目以下の並列 | `cards`（見出し＋一言） | §4-5 |
| 6項目超の並列 | 分割 or グルーピングして `cards`/`matrix` | §1 1メッセージ |
| 時系列の推移 | `chart`(折れ線) | §4-1 |
| 長い文章の塊 | **主張をタイトルへ抽出＋根拠を図/表に** | §3-1 Assertion-Evidence |

> **原則**: 箇条書きの段落を本文の主役にしない。**主張（タイトル）＋視覚的根拠**で構成。

---

## §5. 出力フォーマット（slide_plan スキーマ）

判断レイヤーの成果物。**埋め込みが決定的に処理できる構造**であること。

```jsonc
{
  "goal": "solution",            // 人 が宣言（人/会社/ソリューション）
  "recipe": "solution_v1",       // ゴール別レシピID（必須スライド・順序・トーン）
  "slides": [
    {
      "slide_id": "s03",
      "role": "data",            // §1 ロール
      "archetype": "kpi_metric", // §2 語彙
      "title": { "text": "導入後3年でコスト40%減", "is_assertion": true },
      "slots": [
        { "slot_id": "metric_1", "type": "metric", "content": "40%",
          "source": "fact", "verify": true },
        { "slot_id": "caption_1", "type": "text", "content": "3年累計の削減率",
          "source": "expression", "max_chars": 20 }
      ],
      "emphasis_color_target": "metric_1",   // 強調色は1スライド1箇所
      "rationale": "数値1つ→棒グラフでなくKPI（§4-2）",
      "stage0_checks": { "title_is_assertion": true, "color_count": 2,
                         "chars_ok": true, "archetype_matches_message": true }
    }
  ]
}
```

**必須フィールドの意味**
- `source: fact|expression` … 事実か表現か。fact は `verify:true` で検証対象（捏造防止）。
- `emphasis_color_target` … 強調色を当てる唯一のスロット（§4-4）。
- `rationale` … なぜこの archetype か（LEARN・人レビュー用）。
- `stage0_checks` … §6の安い検証結果。

---

## §6. STAGE0 検証（テキスト段階の関門・最重要の節約ポイント）

**レンダリング前に** slide_plan を決定的にチェックする。ここで落とせば
COM描画もビジョンも使わない＝**使用量ほぼゼロで品質ゲート**になる。

| チェック | 種別 | 適用ロール |
|---|---|---|
| タイトルが主張文（体言止めでない） | H | content, data, summary |
| スロット文字数が `max_chars` 以内 | H | 主張系 |
| 1文 ≤50字／本文が文章の塊でない | H | content, data |
| 色数 ≤3・強調色は1箇所 | H | 全 |
| archetype がメッセージ型と一致 | H | data |
| chart はデータ点 ≥3 / table は充填予定 ≥80% | H | data |
| 1スライド1メッセージ | H | 全 |
| ロールに対し archetype が妥当（儀礼系に主張ルール誤適用なし） | H | 全 |
| 必須スライド・順序がレシピ通り | H | 全 |
| 結論先出し・タイトル通読で話が通る | S | 主張系 |

**合格するまで埋め込みに進まない**（段階ゲート）。違反は判断レイヤーに差し戻して再計画。

---

## §7. 新規ケースの扱い（3階層）と「新規③：ユーザー自己解決フロー（ガード付き）」

ユーザーが「新規」と感じる場面は3階層。**生成が要るのは③だけ**。

| 階層 | 中身 | 対応 | デザイン生成 |
|---|---|---|---|
| 新規① 内容だけ新しい | 既存アーキタイプに収まる | コードで埋める | 不要 |
| 新規② 並びが新しい | 既存部品の新しい**組合せ** | コードで**合成**（語彙を組む） | 不要 |
| 新規③ 形そのものが新しい | どの語彙でも表現できない | **新テンプレを起こす** | **ここだけ** |

### 新規③の方針：ユーザー自身がガード付きで起こす
どの archetype も選択条件を満たさない（スコア閾値未満）→ `needs_custom_template`。
そのとき**ユーザー自身が「構造ビルダー」で新テンプレを作れる**（待たされない）。
ただし出力は**納品デッキではなく「空テンプレ（下書き）」**。下記ガードを全通過して初めて本番利用可。

### ガード（この設計の生命線）
- **G1 見た目トークンは固定（最重要）**: フォント・色・余白・行間は**ブランドトークンから選ぶだけ**。
  ユーザーが操作するのは**構造（箱の配置・どのアーキタイプか）**のみ。**見た目をLLM/ユーザーに生成させない** → 新テンプレでも一貫性が崩れない（「見た目固定・中身可変」を維持）。
- **G2 出力＝空テンプレ**: 実データ・数字を含まない。スロット定義（`role / anchored・flow / max_chars / min_font`）だけ。→ **設計時点で捏造ゼロ**。
- **G3 昇格ゲート（機械）**: §6/§9のチェック＋**トークン準拠**＋グリッド整列＋スロットタグ完備＋**ライブラリ整合lint（既存と衝突しないか）**に合格しないと `frozen` にできない。ハードルールは人でなく機械が強制。
- **G4 二状態ライフサイクル**: `draft`（プレビュー専用・ウォーターマーク）→ `frozen`（客先利用可）。**客先納品は frozen 必須**。ユーザー自身が確定操作するが、合否は機械が判定。
- **G5 合成優先・生成最後**: まず②（既存合成）を試し、満たせない時だけ③に進む。生成の頻度を最小化。
- **G6 ライブラリ自動還元（自己治癒）**: `frozen` 時に共有ライブラリ（`archetypes.md`）へ追加。→ **同じ形は二度と③不要**。③が起きるほど将来の③が減る。
- **G7 充填時に事実検証**: テンプレは構造のみ。後でデータを入れる時に通常の fact 検証（§5 `source`）が走る。

### フロー
```
判断レイヤーが語彙に写像できない（新規③）
        │
        ▼
 [構造ビルダー] ユーザーが archetype を組む（見た目はトークン固定＝G1）
        │  出力 = 空テンプレ draft（G2）
        ▼
 [昇格ゲート] 機械チェック（G3）  ──不合格→ ユーザーに差し戻し
        │ 合格＋ユーザー確定（G4）
        ▼
 frozen テンプレ → ライブラリへ自動還元（G6）
        │
        ▼
 以降は通常の「埋めるだけ」経路に合流（充填時に事実検証＝G7）
```

> **これが「ユーザーが選ぶデザイン経路」の正体**: 並列の生成エンジンではなく、
> **見た目固定・構造だけ開放・機械ゲート付きのテンプレ・ビルダー**。
> per-deck で生成器を回すのではなく、**語彙を1枚増やして本線に合流**させる。

---

## §8. 育て方（LEARN ループ）

人の修正が出たら「**判断のどこが外れたか**」を必ず分類して還元する：

| 修正の種類 | 還元先 |
|---|---|
| archetype 選択が違った | §3 選択ロジック／選択条件の閾値 |
| 内容→図解の写像が違った | §4 写像ルール |
| 文字数・短縮が不適切 | §6 `max_chars`（実測で調整） |
| そもそも語彙が足りない | `archetypes.md` に追加（§7） |
| タイトルの主張化が弱い | §3 Step3 のプロンプト |

- 良い slide_plan は **exemplar** として保存し、選択ロジックの正解例に紐づける。
- 区切りで「今回の修正を本書のどこに当たるか還元して commit」を習慣化（最小コストの蓄積）。

---

## §9. 実装の最小手順（次にやること）

1. **`archetypes.md` の語彙に選択条件タグを付与**（§2 表を機械判定可能に）。
2. **slide_plan スキーマを確定**（§5）し、埋め込み側が読む契約にする。
3. **STAGE0 検証を pytest で実装**（§6）。LLM不要・決定的。←最優先（品質ゲート＆節約）。
4. **選択ロジックを LLM プロンプト＋ルールで実装**（§3）。`rationale` を必ず出させる。
5. もらった実績テンプレを1枚、語彙として登録 → **その1枚で提案1本を通す**（事例化）。
6. 人の修正を §8 で還元し、語彙と閾値を育てる。

> **設計思想の再確認**: 見た目は固定・中身だけ可変／判断は語彙からの選択に限定／
> 客観品質は機械（STAGE0＋実測）、主観品質は judge＋人。生成器は不要。
